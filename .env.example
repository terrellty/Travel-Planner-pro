# Cloudflare Worker endpoint used by the app for cross-device sync.
# Use your deployed worker URL (must include https://).
VITE_CLOUD_WORKER_ENDPOINT=https://travel-planner-worker-ai-storage.<your-subdomain>.workers.dev
